/* ============================================================
   MAIN.JS — Initialization, ties everything together
   ============================================================ */

(function () {
  'use strict';

  // All modules self-initialize. This file handles any
  // cross-module coordination needed after DOM is ready.

  document.addEventListener('DOMContentLoaded', function () {
    // Nothing additional needed — each module handles its own init.
    // This file exists as the entry point for future cross-module logic.
  });

}());
